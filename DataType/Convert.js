let number = "123"
let newNumber = Number(number);
let sNumber = 123;
let stNumber = String(sNumber);
let booleans = true;
let booleanNum = Number(booleans)
let num = null;
let NUm = Number(num);
console.table([(newNumber),(stNumber),(booleans),(NUm)]);
console.table([typeof(newNumber),typeof(stNumber),typeof(booleans),typeof(NUm),typeof(num)]);
